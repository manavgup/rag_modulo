# CI Environment Configuration
# This file contains mock values for CI testing and is safe to commit to GitHub

# Mock OIDC Configuration (satisfies Docker Compose warnings)
OIDC_DISCOVERY_ENDPOINT=https://mock-oidc.example.com/.well-known/openid_configuration
OIDC_AUTH_URL=https://mock-oidc.example.com/authorize
OIDC_TOKEN_URL=https://mock-oidc.example.com/token
OIDC_USERINFO_ENDPOINT=https://mock-oidc.example.com/userinfo
OIDC_INTROSPECTION_ENDPOINT=https://mock-oidc.example.com/introspect

# Mock IBM OAuth Configuration
IBM_CLIENT_ID=mock_ibm_client_id
IBM_CLIENT_SECRET=mock_ibm_client_secret

# Mock WatsonX Configuration
WATSONX_APIKEY=mock_watsonx_key
WATSONX_URL=https://mock-watsonx.example.com
WATSONX_INSTANCE_ID=mock_instance_id

# Development Mode Settings
DEVELOPMENT_MODE=true
SKIP_AUTH=true
LOG_LEVEL=INFO

# Mock LLM Provider Keys (for testing)
OPENAI_API_KEY=mock_openai_key
ANTHROPIC_API_KEY=mock_anthropic_key

# Database Configuration (CI defaults)
COLLECTIONDB_HOST=postgres
COLLECTIONDB_PORT=5432
COLLECTIONDB_NAME=rag_modulo
COLLECTIONDB_USER=rag_modulo_user
COLLECTIONDB_PASS=rag_modulo_password

# Vector Database Configuration
VECTOR_DB=milvus
MILVUS_HOST=milvus-standalone
MILVUS_PORT=19530
MILVUS_USER=root
MILVUS_PASSWORD=milvus

# JWT Configuration
JWT_SECRET_KEY=mock_jwt_secret_key_for_ci_testing
RAG_LLM=ibm/granite-13b-chat-v2

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
REACT_APP_API_URL=/api

# File Storage
FILE_STORAGE_PATH=/tmp/rag_modulo_ci

# MLFlow Configuration
MLFLOW_TRACKING_USERNAME=admin
MLFLOW_TRACKING_PASSWORD=admin
