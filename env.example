#IBM watsonx credentials
GENAI_KEY=your-watsonx-key
api_endpoint=https://bam-api.res.ibm.com
GENAI_API=https://bam-api.res.ibm.com

VECTOR_DB=milvus
COLLECTION_NAME=rag_modulo

# Authentication settings
JWT_SECRET_KEY=your-jwt-secret-key  # Required for JWT token generation
IBM_CLIENT_ID=your-ibm-client-id  # Optional: For IBM OIDC authentication
IBM_CLIENT_SECRET=your-ibm-client-secret  # Optional: For IBM OIDC authentication
OIDC_DISCOVERY_ENDPOINT=your-oidc-discovery-url  # Optional: For OIDC configuration
OIDC_AUTH_URL=your-oidc-auth-url  # Optional: For OIDC authentication
OIDC_TOKEN_URL=your-oidc-token-url  # Optional: For OIDC token endpoint
OIDC_USERINFO_ENDPOINT=your-oidc-userinfo-url  # Optional: For OIDC user info
OIDC_INTROSPECTION_ENDPOINT=your-oidc-introspection-url  # Optional: For token introspection

# Embeddings
EMBEDDING_MODEL=sentence-transformers/all-minilm-l6-v2
EMBEDDING_DIM=384
EMBEDDING_FIELD=embedding  # Name of the field used across vector DBs for embedding purposes
UPSERT_BATCH_SIZE=100 # Unused for now

# Chunking Strategy
CHUNKING_STRATEGY=fixed # 'fixed' or 'semantic'
MIN_CHUNK_SIZE=100
MAX_CHUNK_SIZE=1000
CHUNK_OVERLAP=100
SEMANTIC_THRESHOLD=0.5

# Models
TOKENIZER=meta-llama/llama-3-8b
MODEL=google/flan-t5-xl

# Frontend variables
REACT_APP_API_URL=http://localhost:8000

# Vector DB configurations. Modify only the ones you will be using.
CHROMADB_HOST=localhost
CHROMADB_PORT=8000

ELASTIC_HOST=localhost
ELASTIC_PORT=9200
ELASTIC_PASSWORD=elastic-password
ELASTIC_CACERT_PATH=/Users/mg/mg-work/manav/work/ai-experiments/rag_modulo/http_ca.crt
ELASTIC_CLOUD_ID=''
ELASTIC_API_KEY=

PINECONE_API_KEY=pinecone-key
PINECONE_CLOUD=aws # if aws 
PINECONE_REGION=us-east-1 # region

MILVUS_HOST=milvus-standalone
MILVUS_PORT=19530
MILVUS_USER=MILVUS_USER
MILVUS_PASSWORD=MILVUS_PASSWORD
MILVUS_INDEX_PARAMS=
MILVUS_SEARCH_PARAMS=

WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_GRPC_PORT=50051
WEAVIATE_USERNAME=username
WEAVIATE_PASSWORD=password
WEAVIATE_INDEX=test_weaviate_index
WEAVIATE_SCOPES=None
PROJECT_NAME=rag_modulo
PYTHON_VERSION=3.11

#Local data directory. For testing purposes only
DATA_DIR=/Users/mg/mg-work/manav/work/ai-experiments/rag_modulo/data
